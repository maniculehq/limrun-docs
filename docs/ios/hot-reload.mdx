---
title: "~~Sync with Hot Reload ~~ ~~- When to use Hot Reload (local Mac build → remote sim, vs `lim xcode~~ ~~  build` for remote rebuild)~~ ~~- Sync a built `.app` bundle via the SDK (Ios.createInstanceClient(...).syncApp)~~ ~~- Sync a built `.app` bundle via the CLI (`lim ios sync`)~~ ~~- Watch mode (auto-resync on file changes)~~ ~~- Launch behavior (ForegroundIfRunning vs RelaunchIfRunning)~~ ~~- xdelta3 patching for fast incremental updates~~ ~~- Stream app logs while iterating (streamAppLog, streamSyslog)~~ ~~- Tune sync (basis cache, max patch bytes, install on/off)~~"
description: "~~Sync with Hot Reload ~~ ~~- When to use Hot Reload (local Mac build → remote sim, vs `lim xcode~~ ~~  build` for remote rebuild)~~ ~~- Sync a built `.app` bundle via the SDK (Ios.createInstanceClient(...).syncApp)~~ ~~- Sync a built `.app` bundle via the CLI (`lim ios sync`)~~ ~~- Watch mode (auto-resync on file changes)~~ ~~- Launch behavior (ForegroundIfRunning vs RelaunchIfRunning)~~ ~~- xdelta3 patching for fast incremental updates~~ ~~- Stream app logs while iterating (streamAppLog, streamSyslog)~~ ~~- Tune sync (basis cache, max patch bytes, install on/off)~~ — part of Remote iOS Development."
---

<!-- manicule-placeholder -->

This page is part of the IA scaffold. The Manicule pipeline fills it in
on the next `manicule start` run for this section.
